toystore/
└── backend/
    ├── config/                 # Cấu hình Sequelize + DB
    │   └── db.config.js
    ├── models/                 # Sequelize models (auto sinh từ DB)
    │   └── index.js
    ├── controllers/            # Xử lý logic từng route
    ├── routes/                 # Các route riêng từng phần (auth, product, order...)
    ├── middlewares/           # Auth, error handling, validate request
    ├── utils/                 # Helper functions
    ├── .env                   # Chứa biến môi trường
    ├── server.js              # File chạy chính
    ├── package.json           # Khai báo dependencies
    └── README.md
